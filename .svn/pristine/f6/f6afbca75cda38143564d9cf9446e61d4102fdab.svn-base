import { TransactionPassbook } from "./transactionPassbook";

export class PassBook {
    pageIndex: number;
    pageSize: any[];
    listTransaction: TransactionPassbook[];

    constructor(_index, _pageSize, data) {
        this.pageIndex = _index;
        this.listTransaction = data;
        this.pageSize = _pageSize;
    }
}