import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SelectBankAccountByNumberComponent } from './select-bank-account-by-number.component';

describe('SelectBankAccountByNumberComponent', () => {
  let component: SelectBankAccountByNumberComponent;
  let fixture: ComponentFixture<SelectBankAccountByNumberComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SelectBankAccountByNumberComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SelectBankAccountByNumberComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
