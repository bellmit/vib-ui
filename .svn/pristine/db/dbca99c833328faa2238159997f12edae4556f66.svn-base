import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CardIdNumberComponent } from './card-id-number.component';

describe('CardIdNumberComponent', () => {
  let component: CardIdNumberComponent;
  let fixture: ComponentFixture<CardIdNumberComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CardIdNumberComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CardIdNumberComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
