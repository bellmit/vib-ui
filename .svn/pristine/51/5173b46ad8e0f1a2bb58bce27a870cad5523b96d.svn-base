import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CardIdCardComponent } from './card-id-card.component';

describe('CardIdCardComponent', () => {
  let component: CardIdCardComponent;
  let fixture: ComponentFixture<CardIdCardComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CardIdCardComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CardIdCardComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
